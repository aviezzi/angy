@page "/category/edit/{MicroId:guid}"
@page "/category/create"

@inherits CategoryDetailComponent

<Loader Result="@IsValid">
    <EditForm EditContext="@EditContext" OnValidSubmit="HandleSubmit">
    
        <DataAnnotationsValidator/>
    
        <div class="form-group">
            <label for="name">Nome</label>
            <InputText id="name" class="form-control" aria-describedby="nameHelp" placeholder="Inserire il nome del prodotto" @bind-Value="@Category.Name"/>
            <ValidationMessage For="(() => Category.Name)" />
    
        </div>
    
        <div class="form-group">
            <label for="description">Descrizione</label>
            <InputTextArea id="description" class="form-control" aria-describedby="descriptionHelp" placeholder="Inserire la descrizione del prodotto" @bind-Value="@Category.Description" rows="5"/>
            <ValidationMessage For="@(() => Category.Description)" />
        </div>
    
        <button type="submit" class="btn btn-outline-success w-25 float-right mt-2">Salva</button>
    
    </EditForm>
</Loader>